import React, { useState } from 'react';

const Cart = props => {
    const [items,setItems] = useState([]);

    return (
        <div>
            <h1>placeholder carrito</h1>
        </div>
    );
};

export default Cart;